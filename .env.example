# Environment Configuration Template for AI-Powered Tuxemon
# Austin Kidwell | Intellegix | Mobile-First Pokemon-Style Game
#
# Copy this file to .env for local development
# For Render deployment, set these variables in the Render dashboard

# ============================================================================
# DATABASE CONFIGURATION (Required)
# ============================================================================

# PostgreSQL Database (provided by Render PostgreSQL addon)
DATABASE_URL=postgresql://user:password@host:port/database
# Example: postgresql://tuxemon_admin:password@dpg-xxxxx.oregon-postgres.render.com:5432/tuxemon

# Redis Cache (provided by Render Redis addon or Upstash)
REDIS_URL=redis://host:port
# Render Redis Example: redis://red-xxxxx:6379
# Upstash Example: redis://default:password@host:port

# Qdrant Vector Database (Qdrant Cloud - free tier available)
QDRANT_URL=https://your-cluster.qdrant.io:6333
QDRANT_API_KEY=your-qdrant-api-key

# ============================================================================
# SECURITY CONFIGURATION (Required)
# ============================================================================

# JWT Secret Key (auto-generated by Render for production)
JWT_SECRET_KEY=your-32-character-secret-key-here
# Generate with: openssl rand -hex 32

# ============================================================================
# AI CONFIGURATION (Optional but recommended)
# ============================================================================

# Claude API Key (optional - enables premium AI features)
CLAUDE_API_KEY=your-claude-api-key
# Get from: https://console.anthropic.com/

# AI Cost Controls
MAX_COST_PER_DAY_USD=50
MAX_CLAUDE_REQUESTS_PER_DAY=1000

# Local LLM Configuration (fallback when Claude unavailable)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b-instruct
DEFAULT_MODEL_PROVIDER=local  # Options: local, claude, hybrid

# AI Feature Toggles
ENABLE_AI_FEATURES=true
ENABLE_COST_TRACKING=true
ENABLE_LOCAL_LLM_FALLBACK=true

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Environment
DEBUG=false
LOG_LEVEL=INFO
APP_VERSION=1.0.0

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Game Features
ENABLE_ADMIN_ROUTES=true
ENABLE_GOSSIP_SYSTEM=true
ENABLE_COMBAT_SYSTEM=true
ENABLE_SHOP_SYSTEM=true

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================

# API Endpoints (automatically set by Render for frontend)
VITE_API_BASE_URL=https://tuxemon-backend.onrender.com
VITE_WS_BASE_URL=wss://tuxemon-backend.onrender.com

# PWA Configuration
VITE_APP_NAME=AI-Powered Tuxemon
VITE_APP_VERSION=1.0.0
VITE_ENABLE_PWA=true
VITE_ENABLE_OFFLINE=true

# ============================================================================
# DEVELOPMENT ONLY (Not used in production)
# ============================================================================

# Local Development Database (SQLite)
DEV_DATABASE_URL=sqlite:///./tuxemon_dev.db

# Development Servers
DEV_FRONTEND_URL=http://localhost:5173
DEV_BACKEND_URL=http://localhost:8000

# ============================================================================
# RENDER DEPLOYMENT NOTES
# ============================================================================

# For Render deployment:
# 1. PostgreSQL database is automatically provisioned and DATABASE_URL is set
# 2. JWT_SECRET_KEY is auto-generated if using "generateValue: true" in render.yaml
# 3. Set REDIS_URL, QDRANT_URL, QDRANT_API_KEY manually in Render dashboard
# 4. CLAUDE_API_KEY is optional but recommended for full AI features
# 5. Frontend VITE_API_BASE_URL is automatically set from backend service

# External Service Setup:
# - Qdrant Cloud: https://cloud.qdrant.io/ (free tier: 1GB vectors)
# - Redis: Render Redis addon ($7/month) or Upstash (free tier available)
# - Claude API: https://console.anthropic.com/ (pay-per-use)

# Estimated Monthly Costs:
# - Render Backend (Starter): $7/month
# - Render Frontend (Static): Free
# - Render PostgreSQL (Starter): $12/month
# - Render Redis (Starter): $7/month
# - Qdrant Cloud (Free): $0/month
# - Claude API (variable): $5-50/month depending on usage
# Total: $31-81/month + variable AI costs